export function PerplexityAttribution() {
  return (
    <footer className="w-full py-4 text-center text-xs text-muted-foreground">
      <span>Local app · SQLite · not exposed to the internet by default</span>
    </footer>
  );
}